@startuml activity_diagram
skinparam backgroundColor #ffffff
skinparam activityBackgroundColor #ffffff
skinparam activityBorderColor #000000
skinparam activityArrowColor #000000
skinparam fontColor #000000
skinparam swimlaneBorderColor #000000
skinparam activityStartColor #000000
skinparam activityEndColor #000000
skinparam activityBarColor #000000

title Order Lifecycle — Activity Diagram with Swim Lanes

|Client|
start
:Browse menu;
:Add items to cart;
:Enter seat number;
:Choose payment method;
:Click "Place Order";

|System|
:Validate cart & seat;
if (items available?) then (yes)
  :Calculate total price;
  :Process payment (Strategy);
  :Save order to DB (status=PENDING);
  :Return order confirmation;
else (no)
  :Return error "Item not available";
  |Client|
  :See error toast;
  stop
endif

|Client|
:See "Order #N placed!" confirmation;
:View order in My Orders (PENDING);

|System|
:Order visible to Workers;

|Worker|
:See order in Pending list;
:Click "Accept & Deliver";

|System|
:Update status → IN_DELIVERY;

|Client|
:Status badge changes to IN_DELIVERY (on refresh);

|Worker|
:Physically deliver food to seat;
:Click "Mark Delivered";

|System|
:Update status → DELIVERED;

|Client|
:See status DELIVERED;
stop

@enduml